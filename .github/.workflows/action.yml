name: "Bot hosting"

on:
	push:
	schedule:
		- cron: "*/5 * * * *"

jobs:
	bot:
		runs-on: ubuntu-latest
		steps:
			- uses: actions/checkout@v1
			- uses: actions/setup-node@v1
			- run: npm install
			- name: Run client.js
				run: node client.js
